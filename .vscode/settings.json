{
    "explorer.fileNesting.patterns": {
        "*.html": "${basename}.ts,${basename}.scss",
        "*.ts": "${basename}.scss,${basename}.component.scss"
    },
    "files.exclude": {
        "**/node_modules": true
    }
}